// Đã xóa toàn bộ API liên quan đến alerts vì hệ thống chỉ còn trạng thái trạm.

const express = require('express');
const router = express.Router();

// Không còn API nào ở đây.

module.exports = router; 